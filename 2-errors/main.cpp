#include <iostream>

int main(){
    // This will throw compile-time error because we forgot to add semicolon at the end
    
    // std::cout << "Hello" << std::endl

    // Runtime error because we are trying to divide the value with 0

    // int value = 5/0;
    // std::cout << value << std::endl;
}